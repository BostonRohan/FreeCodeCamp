function booWho(bool) {
  if (bool === true || bool === false) {
    return true;
  }
  return false;
}
booWho([false]);

//Psuedo Code
//If statement, ternary operator.
//If the parameter is true or false, return true.
//Else return false